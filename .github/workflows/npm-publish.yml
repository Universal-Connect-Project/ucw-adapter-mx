name: 'Auto-publish NPM Package'

on:
  push:
    branches:
      - main

jobs:
  push_to_registry:
    name: 'Setup, and publish'
    runs-on: ubuntu-latest

    steps:
      - name: 'Check out the repo'
        uses: actions/checkout@v4

      - name: 'Set up Node'
        uses: actions/setup-node@v4
        with:
          registry-url: 'https://registry.npmjs.org'
          node-version: 'lts/*'
          check-latest: true

      - name: 'Dependencies and tests'
        run: |
          npm ci

      - name: 'Modify name of the package.json prior to publishing'
        uses: maxgfr/github-change-json@v0.0.26
        with:
          key: 'name'
          value: '@ucp-npm/mx-adapter'
          path: ./packages/mx-adapter/package.json

      - name: 'Publish to NPM'
        uses: JS-DevTools/npm-publish@v3
        with:
          token: ${{ secrets.NPM_TOKEN }}
